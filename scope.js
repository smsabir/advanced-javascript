function sum(first, second){
    let result  = first + second;
    return result;
}
console.log(result);
const output = sum(3, 7);
console.log(output);

//scope is the are where we can access the declared variable, if we define a variable with let/const it can be only access within the function block. But var is different.